/**
 * Test utilities for dependency injection.
 *
 * Previous utilities (setupTestContainer, mockService) were removed as dead code.
 * If test DI utilities are needed, implement them in individual test files.
 */
